# Used for local/docker-compose dev only. On Vercel this is injected
# automatically at runtime by the "frontend" service's binding to
# "backend" in vercel.json — do not set it manually there.
API_BASE_URL=http://backend:8000
